import { ArrowPathIcon } from '@heroicons/react/16/solid';

export const Spinner = () => (
  <div className="w-full h-full flex items-center justify-center">
    <ArrowPathIcon className="size-6 animate-spin text-satisfactory" />
  </div>
);
